for running website =>{

  go to backend directory and writw npm install 
  same with frontend

  then first run backend using npm start
  
  if server run and connection established with db
  
  open new terminal go to frontend directory and write npm start 

}

for connecting your mongo =>{
    goto backend directory and open .env file and paste your own mongo connection string
}
